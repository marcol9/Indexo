import express from "express";

// TODO: wire up helmet, morgan, express.json(), payments routes,
// notFound + errorHandler middleware (see docs/solution_design.md §3-4).
export const app = express();
